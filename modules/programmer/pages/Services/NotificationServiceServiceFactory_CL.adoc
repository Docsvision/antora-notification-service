= NotificationServiceServiceFactory -- класс

Фабрика регистрации сервисов

* *Пространство имён:* `DocsVision.NotificationService.ObjectModel.Services`
* *Сборка:* `DocsVision.NotificationService.ObjectModel.dll`

== Синтаксис

[source,csharp]
----
public sealed class NotificationServiceServiceFactory : ServiceFactory
----

== Методы

[cols=",",options="header"]
|===
|Имя |Описание

|`GetService(Type)`
a|Получает сервис указанного типа

*Возвращаемое значение*: сервис в виде объекта

.Параметры:
* `serviceType` -- запрашиваемый тип сервиса

|`RegisterFactory(ObjectContext)`
a|Регистрирует фабрику в контексте, позволяя из контекста обращаться к сервисам модуля

.Параметры:
* `objectContext` -- контекст объекта

|===
